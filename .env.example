# Vercel Environment Variables
# Copy these to your Vercel project settings

# Frontend URL (optional, defaults to relative paths)
VITE_API_URL=/api

# Database configuration (for future use with external database)
DATABASE_URL=

# JWT Secret (if implementing JWT authentication)
JWT_SECRET=

# WebAuthn configuration
WEBAUTHN_RP_ID=
WEBAUTHN_RP_ORIGIN=

# Node environment
NODE_ENV=production